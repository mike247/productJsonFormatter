// This is a list of constants (in this case 1)
// It is the unique identifier for products so we can join common products together

const constants = {
  productJoinField: 'productid',
};

module.exports = constants;
